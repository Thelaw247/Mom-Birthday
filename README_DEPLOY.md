# Deployment

Live site: https://dorothy-38-birthday.onrender.com

Render auto-deploys from `main`. The root `index.html` is the social-preview shell; `birthday.html` contains the birthday experience. `app.js` runs the age/stat intro and loads `main-app.js` for the gated quiz and birthday story.
